from setup import generate_waveform

def main() -> None:
    # load experiment settings
    generate_waveform(plot=True)
    
    # connect & setup external instruments
    # run experiment
    return

main()